# settings.py
# 프레임을 추출할 mp4 파일이 들어있는 폴더, 저장 폴더, 프레임 추출 간격(초)을 이 파일에서 관리하세요.

INPUT_DIR = "input_videos"  # mp4 파일이 들어있는 폴더명, 재귀적으로 순회함
OUTPUT_DIR = "frames_output"  # 프레임 이미지 저장 폴더명
FRAME_INTERVAL = 1  # 프레임 추출 간격(초)
